<nav class="breadcrumbs" aria-label="Brotkrumen">
  <a href="/">Übersicht</a>
  <span aria-hidden="true">/</span>
  <span>{{tile.title}}</span>
</nav>

<section class="page-heading">
  <p class="eyebrow">{{tile.subtitle}}</p>
  <h1>{{title}}</h1>
  <p>Diese Kachel ist im neuen Anwendungskern angelegt und wird im nächsten Umsetzungsschritt ergänzt.</p>
</section>

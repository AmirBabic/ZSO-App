<nav class="breadcrumbs" aria-label="Brotkrumen">
  <a href="/">Übersicht</a>
  <span aria-hidden="true">/</span>
  <a href="{{tile.route}}">{{tile.title}}</a>
  <span aria-hidden="true">/</span>
  <span>{{title}}</span>
</nav>

<article class="content">
  <h1>{{title}}</h1>
  {{{contentHtml}}}
</article>
